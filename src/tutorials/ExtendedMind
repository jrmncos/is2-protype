      <form onSubmit={handleSubmit}>
        <FormControl component="fieldset" error={error} className={classes.formControl}>
          <Typography variant="h6" gutterBottom >
            ¿Por qué se compara a los datos con el petróleo?
          </Typography>

          <RadioGroup aria-label="quiz" name="quiz" value={value} onChange={handleRadioChange}>
            <FormControlLabel value="1" control={<Radio />} label="Los datos pueden implicar conocimiento, aunque no es una condición necesaria." />
            <FormControlLabel value="2" control={<Radio />} label="Los datos son adquiridos mediante el conocimiento." />
            <FormControlLabel value="3" control={<Radio />} label="Los datos son la materia prima cognitiva que reemplaza a la mano de obra." />
            
          </RadioGroup>
          <FormHelperText>{helperText}</FormHelperText>
          <Button type="submit" variant="outlined" color="primary" className={classes.button}>
            Check Answer
          </Button>
        </FormControl>
      </form>